void init(int *a, int *b, int *c, int n) {
  for (int i = 0; i < n; i++) {
    c[i] = i + i;
    b[i] = i * i;
  } 

  int diff=0;
  int b1=10,c1=20;
  if(b1>c1)
    diff=b1-c1;
   if(c1>b1)
   diff=c1-b1; 

  for (int i = 0; i < n; i++) {
    a[i] = b[i] + c[i];
  } 
}

